Run [slack-triage](sand-workflow:slack-triage) in mode `urgent-watch` across all connected Slack accounts (Velora/default and tikal). Alert immediately only for urgent pings that need Avishay now. Dismiss noise from triage (mark read in Slack only if a real mark-read API exists). Stay silent when nothing urgent. If a Slack account repeatedly fails auth, pause this routine and tell Avishay which account to reconnect.